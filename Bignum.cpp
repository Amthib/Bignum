#include "Bignum.h"
/*
Bignum operator+(const Bignum& a, const Bignum& b)
{
    Bignum copie(a);
    a += b;
    return a;
}

Bignum operator-(const Bignum& a, const Bignum& b)
{
    Bignum copie(a);
    a -= b;
    return a;
}

Bignum operator*(const Bignum& a, const Bignum& b)
{
    Bignum copie(a);
    a *= b;
    return a;
}

Bignum operator/(const Bignum& a, const Bignum& b)
{
    Bignum copie(a);
    a /= b;
    return a;
}
*/
